---
name: "Which run flag stays on the on-premise instance"
tags: ["on-premise", "cli"]
runs: 2
max_turns: 4
allowed_tools: [Read, Skill]
---

This machine has an EyePop on-premise instance running. When I run inference from the CLI here, which target flag should I use so the run stays on this machine and never bills cloud compute, and which form is the trap?
