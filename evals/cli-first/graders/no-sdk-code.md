---
type: llm
criteria: "The answer leads with a CLI command (eyepop run over the folder, --recursive and --json) and does not present a Python or Node SDK program as the way to do this one-off job. Mentioning the SDK exists as a later option is fine; writing SDK code is not."
target: last_message
---
