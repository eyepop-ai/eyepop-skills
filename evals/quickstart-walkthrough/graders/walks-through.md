---
type: llm
criteria: "The answer installs nothing without the user's agreement, asks the user for media or proposes a file it found and waits for a yes, and explains the run command and the result fields (source size, objects with label, confidence, box) in plain language rather than only printing commands."
target: last_message
---
